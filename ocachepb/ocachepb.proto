syntax = "proto3";

package ocachepb;

option go_package = "./";

message Request {
  string class = 1;
  string key = 2;
}

message Response {
  bytes value = 1;
}

service ClassCache {
  rpc Get(Request) returns (Response);
}
